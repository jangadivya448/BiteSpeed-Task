import Button from "../Button";
import { HeaderComponent } from "./styles";

function Header() {
  return (
    <HeaderComponent>
      <Button fontWeight="600">Save Changes</Button>
    </HeaderComponent>
  );
}

export default Header;
